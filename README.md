# ISE_microfossil

Due to github upload restrictions, the trained model (/models) SO32 data set (/data) virtual environment (/radiolaria) cannot be uploaded

由于github上传限制，无法上传训练好的模型(/models)SO32数据集(/data)虚拟环境(/radiolaria))

